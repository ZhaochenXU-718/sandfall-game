# ART-C09：游戏内 UI 微信/抖音真机构建与验收

状态：已完成三端生产构建、静态产物检查及微信/抖音扫码真机视觉验收。

构建日期：2026-08-30

构建工具：Cocos Creator 3.8.8

## 1. 构建产物

| 平台 | 输出目录 | 实际文件总量 | 关键入口 |
| --- | --- | ---: | --- |
| Web Mobile（视觉预检） | `build/web-mobile-c09/` | 8,741,895 bytes | `index.html` |
| 微信小游戏 | `build/wechatgame/` | 3,951,266 bytes | `game.js`、`game.json`、`project.config.json` |
| 抖音小游戏 | `build/bytedance-mini-game/` | 3,925,610 bytes | `game.js`、`game.json`、`project.config.json` |

- 三项构建均由 Cocos Creator 命令行任务明确输出 `Finished`，退出状态为 Creator 约定的成功码 `36`。
- 微信 AppID 为 `wx80d45d2a90444f56`，抖音 AppID 为 `tt59de2f63c7a5fea202`，均已写入对应 `project.config.json`。
- 微信构建继续使用项目模板中的最高 `2×` 初始渲染倍率限制，与抖音端策略一致。
- 相对 C08 接入前记录的包体，微信与抖音均增加 `28,382 bytes`；新增量一致，没有出现平台侧意外资源复制。

## 2. 资源完整性检查

三个构建的 `assets/resources/native/` 均包含 C08 接入的完整 UI 资源：

- 12 个 `64×64` RGBA 功能图标。
- 2 个 `286×300` RGBA 弹窗装饰。
- 2 个 `280×96` RGBA 等级/连锁反馈装饰。

三端构建后的尺寸和 alpha 格式与 `assets/resources/art/ui/` 源资源一致；Creator 未生成自动图集，资源仍可逐项核对。

## 3. 真机验收清单

以下项目已分别在微信与抖音真机完成确认：

- 首页进入游戏后，暂停、继续、重开、首页图标在小尺寸下语义清晰，像素边缘没有线性模糊。
- SCORE/TIME、NEXT、暂停按钮未被平台胶囊或刘海安全区遮挡。
- 普通亮度与低亮度下，HUD、按钮、弹窗标题和动态数据均保持足够对比度。
- 按下反馈仅下移视觉层 `2` 点，快速连续点击和移出取消后能恢复正确状态，命中区不漂移。
- 暂停与 Game Over 装饰、动态标题、分数、时间和按钮文字无重叠。
- 等级提升与 `×2 / ×3 / ×4+` 连锁反馈颜色、缩放、上移和淡出正确，且不遮挡棋盘操作。
- 微信与抖音各完成一次完整开局、暂停/继续、Game Over、重开与回首页流程。

## 4. 验收结论

2026-08-30，用户确认微信与抖音真机验证完成；小尺寸识别、文字对比度、安全区、低亮度、按钮状态恢复、弹窗数据可读性和等级/连锁反馈均无阻断问题。`ART-C09` 验收通过，批次 C 关闭。
